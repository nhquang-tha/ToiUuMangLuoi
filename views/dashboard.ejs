<%- include('partials/head') %>
<div class="layout">
    <%- include('partials/sidebar') %>
    <main class="main-content">
        <header class="topbar">
            <h1><%= page %></h1>
        </header>
        <div class="content-body">
            <div class="card">
                <h3>Nội dung của <%= page %></h3>
                <p>Khu vực này sẽ hiển thị bảng dữ liệu, biểu đồ hoặc form tương ứng với menu bạn chọn.</p>
            </div>
        </div>
    </main>
</div>
</body>
</html>
